public class Launcher {
    //Zaimplementuj zbiór klas stanowiących bazową mechanikę sklepu internetowego
    // (dostępne towary z nazwami, ilościami i cenami, klienci, zapisy transakcji) i
    // zastosuj następujące wzorce projektowe: DAO (warstwa persystencji),
    // Singleton (główna klasa sterująca aplikacją),
    // Factory (tworzenie różnych podkategorii przedmiotów z jednej metody) oraz
    // Builder (stwórz jeden specyficzny typ przedmiotu, który zawiera bardzo wiele zmiennych)
    public static void main(String[] args) {
        Customer customer1 = new Customer("1");
    }
}
